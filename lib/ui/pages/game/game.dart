import 'package:flutter/material.dart';

class FjGameScreen extends StatelessWidget {
  const FjGameScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("游戏");
  }
}
